@page "/country/{Id:int}/view"

<div class="row justify-content-center">
    <div class="col-8">
        @if (Country != null)
        {
            <div>
                <h2><span>Country</span>@Country.Id</h2>

                <hr>

                @*<jhi-alert-error></jhi-alert-error>*@

                <dl class="row-md jh-entity-details">
                    <dt><span>Country Name</span></dt>
                    <dd>
                        <span>@Country.CountryName</span>
                    </dd>
                    <dt><span>Region</span></dt>
                    <dd>
                        @if (Country.Region != null)
                        {
                            <div >
                                <a href="/region/@Country.Id/view">@Country.Region.Id</a>
                            </div>
                        }
                    </dd>
                </dl>

                <button @onclick="Back" class="btn btn-info">
                    <Icon Name='"arrow-left"' />&nbsp;<span>Back</span>
                </button>

                <button type="button"
                        [routerLink]="['/country', country.id, 'edit']"
                        class="btn btn-primary">
                    <fa-icon icon="pencil-alt"></fa-icon>&nbsp;<span>Edit</span>
                </button>
            </div>
        }
    </div>
</div>